name: 'eth_dbt_cert_prep'
version: '1.0.0'

profile: 'eth_dbt_cert_prep'

flags:
  fail_fast: true

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  eth_dbt_cert_prep:
    +materialized: table
    +tags: ["eth", "crypto"]
    marts:
      +tags:
        - powerbi
        - report
